# Public browser config only. NEVER put secrets in NEXT_PUBLIC_* vars.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Server-side only (used by Server Components / Route Handlers for SSR fetches).
# Inside Docker this points at the api service; locally it matches the public URL.
API_INTERNAL_URL=http://localhost:3001
